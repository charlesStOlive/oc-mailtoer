# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
        invisible: true
    render_mode_html:
        label: waka.mailtoer::wakamailto.render_mode_html
        type: switch
        searchable: false
        sortable: true
    name:
        label: waka.mailtoer::wakamailto.name
        searchable: true
    slug:
        label: waka.mailtoer::wakamailto.slug
        clickable: false
    state:
        label: waka.mailtoer::wakamailto.state
        searchable: true
    updated_at:
        label: waka.utils::lang.global.updated_at
        type: timetense
    sort_order:
        label: waka.utils::lang.global.sort_order
        invisible: true
